/** Home snap panel width — wider home pushes motto / lights toward the right. */
export const HOME_PANEL_VW = 68;

/** Motto panel width (lights animation frame). */
export const MOTTO_PANEL_VW = 50;

/** What-we-do panel width — narrower panel brings projects closer at snap. */
export const WHAT_WE_DO_PANEL_VW = 74;

/** Contact panel width — narrower right edge peeks landing at snap. */
export const CONTACT_PANEL_VW = 36;

/** Default motto background — used for loop preload peek beside home. */
export const MOTTO_DEFAULT_BG = "#5A7482";

/** Landing home panel (hero) background and heading color. */
export const LANDING_HOME_BG = "#F4F4E8";
export const LANDING_HERO_TEXT = "#5A7482";
export const LANDING_HERO_ACCENT = "#FF5252";
/** Hero h1 scale — dot uses 0.28em of this size (see LANDING_HERO_DOT_SIZE). */
export const LANDING_HERO_FONT_SIZE = "clamp(2.85rem, 8.75vw, 7.25rem)";
export const LANDING_HERO_DOT_SIZE = `calc(0.28 * ${LANDING_HERO_FONT_SIZE})`;

/** Contact section palette — aligned with landing canvas + brand dark green type. */
export const CONTACT_BG = "#F4F4E8";
export const CONTACT_FG = "#1F3A32";
